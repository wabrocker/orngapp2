import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.billbrocker.orngapp2',
  appName: 'OrngApp2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
